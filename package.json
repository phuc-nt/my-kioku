{
  "name": "my-kioku",
  "version": "0.1.0",
  "description": "Living personal memory for a diary agent — Obsidian vault is the source of truth, SQLite FTS5 is a disposable index",
  "type": "module",
  "bin": {
    "my-kioku": "src/cli.ts"
  },
  "scripts": {
    "build": "bun build --compile src/cli.ts --outfile dist/my-kioku",
    "test": "bun test",
    "start": "bun run src/cli.ts"
  },
  "dependencies": {
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
